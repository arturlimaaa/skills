# Journal

Append-only. One entry per iteration, written by the loop. Never rewrite or delete an entry,
including one that records a mistake: the record of a wrong turn is what stops the next iteration
repeating it.

The shape of an entry is in `PROMPT.md` step 6.

---
