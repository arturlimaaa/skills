# Needs a human

Everything the loop cannot do itself, in one place. The loop appends here and moves to the next
unblocked checkpoint rather than stopping.

Each entry says what is needed, why nothing else can supply it, and what it unblocks. Delete an
entry when it is done, or mark it and say what happened.

> Order by cost to the person, not by the loop's convenience. The cheapest item that unblocks the
> most is the one they should see first.

---

## 1 · <the thing only a person can decide or do>

**Status: open. <How much it blocks.>**

<What is needed, in one or two sentences.>

**Blocks:** <the checkpoint ids, and whether it blocks the whole loop or one item.>

---

## 2 · The operator gates

**Status: open. Each is independently useful.**

Ordered by cost to you:

| Checkpoint | What it needs | Time |
|---|---|---|
| `<id>` | <what> | <how long> |

<Name the cheapest one and say what it buys, so there is an obvious place to start.>

---

## 3 · <a decision that is documentation only>

**Status: open. Nothing is blocked.**

<Findings the loop was not authorised to fix. A document that describes a narrower system than the
one that now exists is not broken, but it is wrong, and only the owner can decide how to reconcile
it.>
